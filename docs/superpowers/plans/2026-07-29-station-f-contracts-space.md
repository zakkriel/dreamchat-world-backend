# Station F — Contracts + Space Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give the world geometry and physics. Nested coordinates + real distance, real move duration (distance ÷ speed, tension-budget-blocked), ObjectRelocated volume-blocking and eager recursive encumbrance, Portal traversal as an accessibility gate, and mint validation + persistence — so "I approach the bar and order a beer" decomposes and resolves as two real actions, the locked cellar hatch actually stops you, and grabbing the black-hole pack pins you where you stand.

**Architecture:** Every deliverable is the engine acquiring a *reader* for vocabulary Station E's foundations seeded but nothing consumed. Blocker-only throughout (contract arithmetic BLOCKS impossibilities, never awards success — FINAL-action-contracts §0). Measurements in state, verdicts computed at ask-time (no `has_`/`can_`/`is_allowed_` columns, ever). SQL owns the deterministic arithmetic (distance, duration, volume, weight); Go orchestrates and consumes the tension budget across the chain; the verdict stage gains the mint's three nets. Location goes from flat (scene id only) to nested (coordinate-in-parent + actor coordinate-in-scene) without an LLM in the distance path.

**Tech Stack:** Go (`core/api`, package main), PostgreSQL (dbmate migrations + pgTAP `core/db/tests/`), existing bridge/driver registry. Units fixed system-wide: **meters, seconds, kilograms** (speeds m/s).

## Global Constraints (the fence — every task implicitly includes these)

- **Scope = FINAL-action-contracts.md, exactly.** "If a computation is not in this document, the engine cannot do it." Anything not in §1–§8 routes to the resolution LLM. Do not add engine powers beyond the contract table.
- **Blocker-only (founder-locked §0).** The gate and contract arithmetic exist to BLOCK impossibilities. Success = the absence of a blocker. Never read "the contract computes the move" as "the move succeeds" — the world still gets its word first (world-first), and opposition is always the resolution LLM, never arithmetic.
- **Measurements, not verdicts (founder-locked §0).** Every persisted property is a measurement (`max_room`, `occupied_room`, `max_load`, `carried_weight`, `base_speed`, `coordinates`). Verdicts (`has-room`, `within-load`, `fits-time`) are arithmetic run at ask-time, never stored. Nouns for state, verbs for checks. Statuses (`encumbered`) ARE legal state — conditions with provenance and a lifetime, inputs to arithmetic, never pre-cached answers.
- **The two-tier wall (§5.2).** Tier-1 (engine-known, closed set in `core/api/tier1.go`) is read by engine checks and strictly validated. Tier-2 is LLM-invented, carried in payloads, NEVER read by the engine. The engine may only read attributes named in the contract. Rule B: the LLM may not write Tier-1 by invention — `locked`/`open`/`connects`/`size`/`weight`/coordinates change only through committed, shape-validated rulings.
- **Units fixed:** meters, seconds, kilograms. A mint in other units is invalid data (§8).
- **Mint validation = shape + derivable bounds only (§8).** No plausibility bounds ("is 400 m/s too fast?"). Trust comes from the three nets: a mint only happens inside an adjudicated ruling past the reality check; blast radius is one logged row with provenance; every mint is audit-trailed.
- **D-1:** only the SQL commit path writes canon. Go orchestrates; SQL commits and computes the deterministic arithmetic. Append-only canon.
- **Accepted imprecisions — do NOT "fix" (they are founder-locked closed decisions):** (a) cross-level distance ignores the walk-to-the-exit-point part — fixing it needs exit-point inference = an LLM = forbidden on this path (§3); (b) Portal is accessibility, NOT geometry — no exit points, no doorway positions, no distance contribution (§5.3); (c) encumbrance slowing (weight → speed penalty beyond the −100% encumbered stop) is explicitly NOT built (§4); (d) throwing has no contract → resolution LLM (§4); (e) over-budget move = REJECT, never clock-jump except under `none` tension (§2). An agent who "improves" any of these is reopening a closed decision.
- **Harness:** dbmate `-- migrate:up`/`-- migrate:down`; `schema.sql` regenerated by `make migrate`, never hand-edited; every migration paired with the regenerated `schema.sql` in the same commit (`make schema-check` is a git-diff exit-code guard). pgTAP house style (`plan(N)`/`finish()`, fresh labels, `BEGIN…ROLLBACK`, self-contained). Go from `core/api/`. New pgTAP files continue at `110_`+; migrations continue after `20260726100001_display_name_naming_reach.sql`.
- **Battery per task:** `make reset && make test` · `cd core/api && go test -count=1 ./...` (re-run TestRunBeat/TestBeat for stability) · `make schema-check` · `make schema-contract`.
- **TDD failing-test-first; one commit per task minimum; plain comments; every new rule in code ships with its worked example (the contract doc's own discipline). One station = one PR.**
- **Station-D incident lesson (binding):** every commit names its exact files in `git add` — never a broad `git add .`/`-A`.

## Program-map compliance (Station F row → tasks)

| Map item | Task |
|---|---|
| F1 coordinates + fn_distance | Task 1 |
| F2 fn_move_duration real | Task 2 |
| F3 tension budget cumulative | Task 3 |
| F4 volume/weight + eager encumbered | Task 4 |
| F5 Portal check | Task 5 |
| F6 mint validators (+ persistence, see decision 4) | Task 6 |
| Seed spatial layout + container (playability) | Task 7 |
| Exit: founder walks to the bar, through the door, at the locked hatch | Task 8 |

Tasks 7–8 make the contract arithmetic playable and gate it — same discipline as Stations D/E (a seed + a founder exit). They extend the map row's delivery, they do not shrink or renegotiate it.

## Flagged plan-level decisions (grounded in the spec; founder may veto)

1. **One tick = one second.** The engine's `in_world_tick`/duration `bigint` is seconds. `fn_move_duration` returns seconds; tension budgets are seconds; no conversion layer. Grounded: the stub returns `5`, frantic budget is `5 s`, and the foundations map ties them. Stated once in code; no unit drift.
2. **Coordinates + parent edge live in the existing `attrs` JSONB, as Tier-1 measurements.** Actor position-within-scene → `actor_state.attrs.coordinates` (registered Tier-1 `coordinates:object`); a location's coordinate-within-parent → `location_state.attrs.coordinates`, and its parent → a new Tier-1 name `parent_location_id` (string). This follows the shipped `attrs`-JSONB measurement pattern (the Tier-1-DDL-vs-JSONB fork stays open per the foundations' flagged decision; F does not resolve it, it continues the JSONB convention with running-code evidence). Coordinate shape: `{"x":<number>,"y":<number>}` in meters within the parent's local frame; the parent's `extent` (`{"w":<number>,"h":<number>}`) bounds mint validation.
3. **`volume` is DERIVED, never stored.** `volume(size) = 4^(size-1)` computed at ask-time from `size` (§4). It does NOT enter `tier1Registry` — a stored volume would be a cached measurement of a measurement, and the room-check reads `size`. This resolves recon's "missing name": nothing is missing.
4. **F6 includes mint PERSISTENCE, not just validation.** Today the `Mints` ruling slot is decoded but inert — no committed path inserts a movement type, status modifier, or typed artifact. F2/F4 arithmetic is only exercised against the two seeded rows until real minting works. F6 therefore (a) validates the three nets at the verdict stage, (b) enforces mint-ordering, and (c) persists accepted mints into `movement_type`/`status_modifier`/`entity_registry`+state, audit-trailed to the ruling event. This is the honest scope of "mint validators" given the inert slot.
5. **The seed's coordinates are hand-authored — a sanctioned test artifact.** §3 says coordinates are LLM-minted, never hand-authored, and "the hand-placed seed world is a test artifact, not the pattern." The Drowned Lantern seed gets hand-authored coordinates + a container instance under exactly that license (the same license the whole seed already holds). Production worlds mint coordinates via F6.
6. **`fn_move_duration` keeps its `(uuid,uuid,uuid)→bigint` signature.** The legacy SQL `apply_beat` path still calls it and is still tested; F2 changes its BODY (flat-5 → real arithmetic), not its signature, so `apply_beat` keeps working and its tests absorb the new real durations. The live path is the Go orchestrator; `apply_beat` is legacy-but-green.
7. **Actor active statuses get a real representation (F2), written eagerly by F4.** `effective_speed` needs to know which modifier statuses an actor currently has. F2 introduces `actor_state.attrs.statuses` (array of `status_type_id`) as the read model; `effective_speed` intersects it with `status_modifier` rows. F4's eager rule writes/clears `encumbered` in that array in the same commit as the carry change. `encumbered` present → −100% → speed 0 → move blocked, purely by arithmetic (no special "tied" rule).

## File structure

- Migrations (continue after `20260726100001_…`): `…F1_coordinates_distance.sql`, `…F2_move_duration.sql`, `…F3_*` (Go-only, no migration), `…F4_object_relocated.sql`, `…F5_portal_traversal.sql`, `…F6_mint_persistence.sql` — each paired with regenerated `schema.sql`.
- pgTAP: `110_fn_distance_test.sql`, `111_fn_move_duration_test.sql`, `112_tension_budget_test.sql` (may be Go-side), `113_object_relocated_test.sql`, `114_portal_traversal_test.sql`, `115_mint_persistence_test.sql`, `116_drowned_lantern_space_test.sql`.
- Go: `core/api/orchestrator.go` (tension budget consumption, clock stage), `core/api/tension.go` (new — enum→seconds + budget), `core/api/verdict.go` + new `core/api/mint.go` (three nets + persistence), `core/api/tier1.go` (`parent_location_id`), plus tests.
- Seed: `core/db/seeds/seed_drowned_lantern.sql` (coordinates + a container + weights).
- Runbook: `docs/runbooks/station-f-exit.md`.

**Branch:** `feat/station-f-contracts-space`, off a `main` that already contains Station E (open + merge E's PRs first — the base every prior station used). If E is not yet merged, branch off `feat/station-e-cognition` and rebase onto main after E lands.

---

### Task 1: Nested coordinates + `fn_distance`

**Files:**
- Create: `core/db/migrations/20260729100001_coordinates_distance.sql`
- Modify: `core/api/tier1.go` (add `parent_location_id`)
- Test: `core/db/tests/110_fn_distance_test.sql`

**Interfaces:**
- Produces (consumed by Task 2):
  - `fn_location_depth(p_world_id uuid, p_location uuid) RETURNS int` — breadcrumb depth (root = 0), walking `location_state.attrs->>'parent_location_id'`.
  - `fn_nearest_common_parent(p_world_id uuid, p_a uuid, p_b uuid) RETURNS uuid` — the deepest location that is an ancestor-or-self of both entities' locations. For two actors/things, "their location" = the scene they're in (`actor_state`/`location_state` as applicable); walk parents to equal depth then together until they meet.
  - `fn_distance(p_world_id uuid, p_a uuid, p_b uuid) RETURNS numeric` — Euclidean distance in meters between `p_a` and `p_b` at their nearest common parent's local frame. Same-scene things: distance between their in-scene `coordinates`. Cross-level: distance between the two *child locations'* coordinates at the common parent (accepted imprecision §3 — do NOT add exit-point inference; comment must say so with the "15 seconds of barroom inside a 10-minute walk" reasoning). NULL/missing coordinate → treat as origin `{x:0,y:0}` of its frame (comment: pre-spine rows converge on mint; the seed hand-authors all coordinates).
  - Tier-1: `parent_location_id` (string) added to `tier1Registry`.

- [ ] **Step 1: Failing pgTAP** `110_fn_distance_test.sql` (`plan(5)`, house style): fixture = world; parent location "district" (coord `{x:0,y:0}`, extent `{w:2000,h:2000}`); two child locations "tavern" (coord `{x:100,y:0}`, parent=district) and "alley" (coord `{x:900,y:0}`, parent=district); actor A at tavern coord `{x:2,y:0}`, bar (artifact) at tavern coord `{x:14,y:0}`. Assert: (a) `fn_location_depth(district)=0`, `fn_location_depth(tavern)=1`; (b) `fn_nearest_common_parent(A, bar)=tavern` (same scene); (c) `fn_distance(A, bar)=12` (same-scene coords, 14−2); (d) `fn_nearest_common_parent(tavern, alley)=district`; (e) `fn_distance(tavern, alley)=800` (child-location coords at the district frame). Write each block self-contained.
- [ ] **Step 2: Run** `make reset && make test` → 110 FAILS (functions missing).
- [ ] **Step 3: Migration** — three `CREATE FUNCTION … LANGUAGE sql STABLE`. `fn_location_depth`: recursive CTE over `parent_location_id`. `fn_nearest_common_parent`: resolve each entity to its scene (an actor → `actor_state.attrs->>'location_id'`; a location → itself; an artifact → its holder's/location's scene via `attrs->>'location_id'` if present else its registry `current_scene_id`), then climb to equal depth and together until equal. `fn_distance`: locate both at the common parent's frame (same-scene → their own `coordinates`; else → the ancestor-child-location coordinates), `sqrt((ax-bx)^2 + (ay-by)^2)`. Down: drop all three. Add `parent_location_id` to `tier1Registry` (with a comment: a location's parent edge, Tier-1 string).
- [ ] **Step 4: `make migrate` → full battery** green (existing 109-and-below untouched; the new tier1 name must not break `verdict_test.go` — run it).
- [ ] **Step 5: Commit** — exact files — `feat(space): nested coordinates + fn_distance (nearest-common-parent, accepted cross-level imprecision) [F1]`

### Task 2: Real `fn_move_duration` + effective speed

**Files:**
- Create: `core/db/migrations/20260729100002_move_duration.sql`
- Modify: `core/db/schema.sql` (via `make migrate`)
- Test: `core/db/tests/111_fn_move_duration_test.sql`; update `core/db/tests/92_fn_move_duration_test.sql`, `95_apply_beat_happy_test.sql`, `96_apply_beat_partial_beat_test.sql`, `core/api/orchestrator_test.go` (durations are no longer flat 5)

**Interfaces:**
- Produces (consumed by Task 3, orchestrator clock):
  - `fn_effective_speed(p_world_id uuid, p_actor uuid, p_movement_type text DEFAULT 'walk') RETURNS numeric` — `base_speed_mps(movement_type) × Π(1 + modifier_percent/100)` over the actor's active statuses (`actor_state.attrs->'statuses'` ∩ `status_modifier` rows for that movement type). Floor at 0 (a −100% modifier → 0; multiple modifiers multiply; **no upper cap**). Comment ships the worked example `walk 1.4 × baby(−90%) × trained(+20%) × limping(−30%) = 0.1176`.
  - `fn_move_duration(p_world_id uuid, p_from uuid, p_to uuid) RETURNS bigint` (signature unchanged) — now `CEIL(fn_distance(from_actor?, to) ÷ effective_speed)`. But `from`/`to` are *locations* in the current callers. Refine: the mover is the actor; duration = `CEIL(fn_distance(p_from_location's representative point for the actor, p_to_location) ÷ speed)`. Since callers pass `(world, here_location, dest_location)`, compute distance between the two locations via `fn_distance` and speed via the actor's statuses — BUT `fn_move_duration` has no actor param. Resolution: add `fn_move_duration_actor(p_world_id, p_actor, p_from, p_to) RETURNS bigint` as the real function; keep `fn_move_duration(world,from,to)` as a thin wrapper assuming `walk` + no statuses (legacy `apply_beat` compatibility, decision 6) so its behavior becomes "distance ÷ 1.4" — deterministic, still tested. The Go orchestrator switches to `fn_move_duration_actor` (it has the actorID). Speed 0 → return a sentinel `9223372036854775807` (max bigint) so "never fits any budget" emerges from arithmetic (§2 "−100% → duration infinite → never fits"), never a special-case.
- Consumes: `fn_distance` (Task 1), `movement_type`/`status_modifier` (seeded), `actor_state.attrs.statuses`.

- [ ] **Step 1: Failing pgTAP** `111_fn_move_duration_test.sql` (`plan(5)`): fixture with a district+tavern+alley (as Task 1) and an actor at the tavern. (a) `fn_effective_speed(actor,'walk')=1.4` (no statuses); (b) add a minted `limping` modifier (−30 on walk) + put `limping` in the actor's `attrs.statuses` → `fn_effective_speed=0.98`; (c) `fn_move_duration_actor(actor, tavern, alley)` = `CEIL(800/1.4)=572`; (d) with `encumbered` in statuses (−100) → `fn_effective_speed=0` and `fn_move_duration_actor` returns max-bigint (blocked-by-arithmetic); (e) legacy `fn_move_duration(world, tavern, alley)=CEIL(800/1.4)=572` (walk, no statuses). 
- [ ] **Step 2: Run** → 111 FAILS; also 92/95/96/orchestrator_test now fail on the flat-5 assumption (expected — they assert 5).
- [ ] **Step 3: Migration** — `fn_effective_speed`, `fn_move_duration_actor`, and the rewritten `fn_move_duration` wrapper. Down restores the flat-5 body (copy from the current `20260723100001_location_ids.sql`).
- [ ] **Step 4: Update the flat-5 dependents honestly.** 92: assert real distances (its fixtures now need coordinates — give the two test locations coordinates so distance is defined; if a test used bare uuids with no coordinates, the origin-fallback makes distance 0 → duration 0; update the assertions to the computed values and say so). 95/96/orchestrator_test: recompute expected ticks from the new arithmetic (derive in-test, comment the derivation). Switch the Go orchestrator clock stage (`core/api/orchestrator.go:165`, wrapper `fnMoveDuration` ~732) to call `fn_move_duration_actor` with the moving actorID.
- [ ] **Step 5: Full battery** green. **Step 6: Commit** — `feat(move): real duration — distance ÷ effective_speed(status modifiers); −100% ⇒ blocked by arithmetic [F2]`

### Task 3: Cumulative tension budget

**Files:**
- Create: `core/api/tension.go`
- Modify: `core/api/orchestrator.go` (replace the flat `beatTickCap` backstop with the tension budget, consumed cumulatively)
- Test: `core/api/tension_test.go`, and an integration case in `orchestrator_test.go`

**Interfaces:**
- Produces:
  - `func tensionBudgetSeconds(tension string) int64` — `frantic:5, tense:30, normal:60, calm:600, none:` → sentinel `math.MaxInt64` (∞). Unknown → treat as `none` (the scene always has a validated enum; defensive). Ships the enum table as a comment.
  - Orchestrator: the beat reads the scene's current `location_state.attrs.tension` at the start (via a small query; `loadScene` already reads tension for the cognition prompt — reuse). A running `budgetRemaining int64` starts at `tensionBudgetSeconds`. Each ActorMoved's `fn_move_duration_actor` result is subtracted; if a move's duration `> budgetRemaining` → the chain HALTS there with `HaltReason="turn_budget"`, the prefix stands (cumulative rule §6 — three 12s moves in a 30s beat: 12,24 pass, 36 rejects). Non-move passthrough/adjudicated steps consume their own duration if they have one (v1: only moves have computable duration; others consume 0 — comment that Way-A long-action accumulation is Station-later). `none` → `budgetRemaining` never decrements (the atomic time-skip; §5). Replaces the `beatTickCap=1000` constant as the real backstop (keep a sanity max only as a runaway guard, comment it as belt-and-suspenders).
- **Boundary:** the budget is a BLOCKER (one impossibility — not enough time — didn't fire), never an award (§6). The world still acts first; do not read "fits" as "happens."

- [ ] **Step 1: Failing tests** `tension_test.go`: table test of `tensionBudgetSeconds` (all five values + unknown); and (DB harness) `TestRunBeat_TensionBudgetCumulative` — scene `tense` (30s), a chain of three moves each ~12s across a seeded geometry → first two commit, third halts `turn_budget`, exactly two ActorMoved canon rows. A `none` scene → a long move commits (no budget block).
- [ ] **Step 2: Run** → FAIL (tension.go missing; orchestrator still uses beatTickCap).
- [ ] **Step 3: Implement** `tension.go` + rewire the orchestrator clock/halt. Keep `apply_beat`'s SQL `p_tick_cap` as-is (legacy path, not the live budget) — the Go orchestrator owns the tension budget.
- [ ] **Step 4: Full battery** green (existing TestRunBeat cases: give their fixtures a scene tension or default to `none` so they don't spuriously budget-halt — check each and adjust the fixture, not the assertion, naming them).
- [ ] **Step 5: Commit** — `feat(tension): cumulative beat budget (enum→seconds); over-budget move REJECTs, prefix stands [F3]`

### Task 4: ObjectRelocated — volume blocks, weight consequences (eager encumbrance)

**Files:**
- Create: `core/db/migrations/20260729100003_object_relocated.sql`
- Modify: `core/db/schema.sql`, and the `apply_event`/`apply_ruled_event` ObjectRelocated floor + commit (both twins, keep in sync)
- Test: `core/db/tests/113_object_relocated_test.sql`

**Interfaces:**
- Produces:
  - **Containment model:** a container's contents = entities whose `attrs->>'held_by'`/`carried_by` (unify to one Tier-1 key `contained_by`, string, added to registry) points at the container. Migration converts the seed's Tier-2 `carried_by`/`held_by` to Tier-1 `contained_by` (decision: carry becomes engine-readable state — it must, for the eager rule; comment cites §4). Actors are the root carriers.
  - `fn_effective_weight(p_world_id uuid, p_entity uuid) RETURNS numeric` — recursive: `(empty_weight_or_weight + Σ fn_effective_weight(contents)) × COALESCE(weight_modifier,1)` (§4 container formula). A plain object = its `weight`. Comment ships the waterlogged-pack worked example (`(2 + 160)×1.0 = 162`).
  - `fn_volume(size int) RETURNS numeric` = `4^(size-1)` (derived, §4; NOT stored).
  - **Volume floor (blocker):** ObjectRelocated into a container checks `occupied_room + fn_volume(object.size) ≤ max_room` (computed, never stored). Fail → `gate_reject`, nothing written (a size-5 crate into a size-2 pouch). Untyped destinations (a location, an actor's hands) have no room check in v1 (only Container instances carry `max_room` — comment).
  - **Eager encumbrance (§4, in the SAME commit):** on any committed ObjectRelocated that changes a carry chain, recompute `carried_weight` for every affected carrier recursively up the chain; write `carried_weight` (measurement) and set/clear `encumbered` in `attrs.statuses` where `carried_weight > max_load`. Provenance = the relocation event. This makes F2's speed read a true encumbered state instantly.
- Consumes: `size`/`weight`/`max_room`/`occupied_room`/`empty_weight`/`max_load`/`carried_weight` (Tier-1, now with readers), `attrs.statuses` (Task 2).

- [ ] **Step 1: Failing pgTAP** `113_object_relocated_test.sql` (`plan(6)`): (a) `fn_volume(1)=1`, `fn_volume(5)=256`; (b) relocate a size-5 object into a `max_room=4` container → `gate_reject`, nothing written (volume blocks); (c) relocate a size-1 object into `max_room=4`, `occupied_room=0` → committed, `occupied_room` now reflects it; (d) `fn_effective_weight` of the waterlogged pack (2 empty, 4×40 contents) = 162; (e) actor `max_load=80` grabs that pack → committed, `carried_weight=162`, `encumbered` ∈ `attrs.statuses`; (f) actor drops it → `encumbered` cleared, `carried_weight` back to 0. 
- [ ] **Step 2: Run** → 113 FAILS. **Step 3: Migration** — `fn_effective_weight`, `fn_volume`, the volume-floor addition + eager-encumbrance block in both `apply_event` and `apply_ruled_event` ObjectRelocated branches (shared plpgsql helper `fn_apply_carry_change(event_id, world, object, old_holder, new_holder)` to avoid twin drift — the recon flagged the twin "keep in sync" comment; a shared helper is the DRY fix). Convert seed `carried_by`/`held_by` → `contained_by`; add `contained_by` to `tier1Registry`. Down: restore existence-only floor + the Tier-2 carry keys.
- [ ] **Step 4: Full battery** green (94/95/96 relocation-adjacent tests; the seed's art_note/cellar-key carry now Tier-1 — 109 seed test may assert the old Tier-2 keys, update it). **Step 5: Commit** — `feat(relocate): volume-blocks + eager recursive encumbrance; carry becomes Tier-1 contained_by [F4]`

### Task 5: Portal traversal check

**Files:**
- Create: `core/db/migrations/20260729100004_portal_traversal.sql`
- Modify: `apply_event`/`apply_ruled_event` ActorMoved floor (twins), `core/api/orchestrator.go` `premiseHolds` ActorMoved mirror
- Test: `core/db/tests/114_portal_traversal_test.sql`, a Go premise case

**Interfaces:**
- Produces:
  - `fn_portal_permits(p_world_id uuid, p_from uuid, p_to uuid) RETURNS boolean` — TRUE iff a Portal artifact exists whose `connects` (Tier-1 array) contains both `p_from` and `p_to` AND `open=true` AND `locked=false` (§5.3, v1 passage rule). No portal between them → FALSE (not adjacent). Comment: this makes the locked door real for everyone, computed fresh (no stored `reachable`).
  - **Wired into the ActorMoved accessibility floor** in both commit twins: destination exists (as today) AND `fn_portal_permits(here, dest)`. Fail → `gate_reject`. Same-scene moves (no location change) skip the portal check (moving to the bar within the tavern is not a traversal). The Go `premiseHolds` ActorMoved case mirrors it (comment: floor/premise parity).
- **Boundary (§5.3):** Portal is accessibility ONLY. Do NOT let it contribute to `fn_distance` or coordinates. An agent adding exit-point geometry is reopening a closed decision.

- [ ] **Step 1: Failing pgTAP** `114_portal_traversal_test.sql` (`plan(4)`): fixture = tavern + cellar + a locked cellar-hatch portal (connects [tavern,cellar], open=false, locked=true); tavern + dock-street + an open front-door portal. (a) `fn_portal_permits(tavern, dock_street)=true`; (b) `fn_portal_permits(tavern, cellar)=false` (locked); (c) ActorMoved tavern→cellar via `apply_event` → `gate_reject`, no canon; (d) ActorMoved tavern→dock_street → committed.
- [ ] **Step 2: Run** → 114 FAILS. **Step 3: Migration** + floor wiring + Go mirror. Down: drop `fn_portal_permits`, restore existence-only ActorMoved floor. **Step 4: Full battery** — existing ActorMoved tests now need a permitting portal between their test locations (or must be same-scene moves); update those fixtures, name them (this is the big blast radius — every move test that crossed locations without a portal). **Step 5: Commit** — `feat(portal): traversal accessibility gate on ActorMoved (open ∧ ¬locked); reachable computed, never stored [F5]`

### Task 6: Mint validation + persistence (the three nets)

**Files:**
- Create: `core/api/mint.go`, `core/db/migrations/20260729100005_mint_persistence.sql`
- Modify: `core/api/verdict.go` (call the mint validator), `core/api/orchestrator.go` (persist accepted mints in the ruling tx)
- Test: `core/api/mint_test.go`, `core/db/tests/115_mint_persistence_test.sql`

**Interfaces:**
- Produces:
  - `func validateMints(mints []json.RawMessage, existingMovementTypes map[string]bool) []string` — shape + derivable bounds ONLY (§8): movement type `baseSpeed > 0`; modifier `modifierPercent ≥ -100` (no upper cap); a modifier's referenced `movementTypeId` must exist (seeded or in this ruling's earlier mints — mint-ordering); artifact mint `size ∈ 1..10`, container `max_room ≤ 4^(size-1)`, a coordinate within its parent's `extent`. Units are m/s·kg·m — a value out of unit range is not checkable numerically, so this validates structure, not plausibility (comment: the three nets, not a plausibility bound). Returns violations (empty = pass) → the existing repair-once-then-bounce at the verdict stage.
  - **Persistence** (in the ruling's single tx, after verdict passes): `apply_mint(world, ruling_event_id, mint jsonb)` inserts into `movement_type` / `status_modifier` (with the `status_modifier` movement FK satisfied by ordering) / `entity_registry`+state for a typed artifact, each audit-trailed via `created_by_event = ruling_event_id`. Blast radius = one logged row (§8 net 2).
- Consumes: the verdict stage (`verdictRuling`/`DecodeAndValidateRuling`), the ruling tx (`commitRulingTx`).

- [ ] **Step 1: Failing tests** `mint_test.go`: (a) `baseSpeed:0` → violation; (b) `modifierPercent:-150` → violation; (c) `modifierPercent:900` → PASS (no upper cap); (d) modifier referencing an unknown movement type → violation (ordering); (e) container `max_room:300, size:1` (>4^0=1) → violation; (f) valid `climb`/0.4 mint → pass. `115_mint_persistence_test.sql`: an `apply_mint` of a movement type + a modifier referencing it commits both, audit-trailed to a canon event; a subsequent `fn_effective_speed` reads the minted modifier.
- [ ] **Step 2: Run** → FAIL. **Step 3: Implement** `mint.go` + `apply_mint` + wire into verdict (validate) and `commitRulingTx` (persist). **Step 4: Full battery** green. **Step 5: Commit** — `feat(mint): three-nets validation (shape+bounds, ordering) + audit-trailed persistence of minted physics vocabulary [F6]`

### Task 7: The Drowned Lantern gets space (playability)

**Files:**
- Modify: `core/db/seeds/seed_drowned_lantern.sql` (coordinates, parent edges, a container, weights, actor positions)
- Test: `core/db/tests/116_drowned_lantern_space_test.sql`

**Content (hand-authored test artifact, decision 5):**
- A parent location "Harbor Quarter" (or reuse an existing district) with the tavern/dock-street/alley/cellar as children, each with a `coordinates` in the parent frame and the parent an `extent`. The tavern gets an in-scene frame; the bar (a new artifact or location feature), Mara, Jonas, the hooded woman, and Kade each get `coordinates` within the tavern so `fn_distance(Kade, bar)` is a real few meters.
- The three portals already connect the rooms (Task 5 reads them) — confirm the front door `open=true` so Kade can leave, cellar hatch `locked=true` so he can't descend.
- A **container** instance (Mara's strongbox or a crate) with `max_room`, `empty_weight` — so ObjectRelocated volume/weight has something to exercise; give art_note a real `size:1`/`weight:0` (already), and seed one heavier object to make encumbrance reachable if the founder tries to grab it.
- `seed_world_defaults` already gives walk/encumbered — confirm it's called for the play world.

- [ ] **Step 1: Failing pgTAP** `116_drowned_lantern_space_test.sql` (`plan(5)`): (a) `fn_distance(kade, bar)` is a small positive number (a few meters, not 0/NULL); (b) `fn_move_duration_actor(kade, tavern, tavern)` for an in-scene approach is a few seconds and fits a `tense` (30s) budget; (c) `fn_portal_permits(tavern, dock_street)=true`, `(tavern, cellar)=false`; (d) the container's `max_room` present; (e) all four actors + the bar have coordinates within the tavern.
- [ ] **Step 2: Run → FAIL. Step 3: Author the seed spatial layer. Step 4: Full battery** green (109 seed test + the new 116; schema-contract regenerates payloads against the richer seed). **Step 5: Commit** — `feat(seed): the Drowned Lantern gains geometry — coordinates, portals wired, a container + weights (F playability)`

### Task 8: Exit — the founder walks the room

**Files:**
- Test: `core/api/station_f_exit_test.go`
- Create: `docs/runbooks/station-f-exit.md`
- Modify: `.git/sdd/progress.md` (ledger), PR body

- [ ] **Step 1: The always-run fake E2E** (`TestStationF_FakeE2E`, through the HTTP handler, scripted drivers, high base ticks): a chain "approach the bar, then order a beer" decomposes to two attempts; the in-scene ActorMoved commits with a real (few-second) duration consuming the `tense` budget; a second beat "go down to the cellar" (ActorMoved tavern→cellar) → `gate_reject` (locked hatch); a beat "go out the front" → committed (open portal). Assert canon rows, durations, budget consumption, the portal gate. Deterministic, zero network.
- [ ] **Step 2: Runbook** `station-f-exit.md` (style of `station-e-exit.md`): env, `make reset`, server, play page; the founder's script — walk to the bar (a real move now, not free), order a beer, try the locked cellar hatch (blocked), leave through the front door; what to watch (movement takes beat-budget time; the hatch actually stops you; grabbing something heavy pins you). Troubleshooting: which function to probe (`fn_distance`, `fn_move_duration_actor`, `fn_portal_permits`) when a move feels wrong.
- [ ] **Step 3: FOUNDER GATE:** Brian plays — "approach the bar and order a beer" lands as two actions; the locked hatch blocks; the front door lets him out; a heavy grab encumbers. His verdict lands in the ledger.
- [ ] **Step 4: Ledger + PR** — progress.md Station F section (per-task lines, minors roll-up); one PR `feat/station-f-contracts-space` with the map-item table.

---

## Verification (the station gate)

1. Full battery green: `make reset && make test` (all pgTAP incl. 110–116) · `go test -count=1 ./...` ×2 stable · `make schema-check` · `make schema-contract`.
2. Grep-fence (measurements-not-verdicts): `grep -rniE "has_room|within_load|can_move|is_allowed|reachable\b" core/db core/api` → no stored-verdict columns (all such are computed functions).
3. Distance is LLM-free: `fn_distance`/`fn_move_duration_actor` reference no seat, no LLM.
4. The wall holds: `volume` is derived not stored; Tier-1 additions (`parent_location_id`, `contained_by`) are measurements; `verdictRuling` still enforces the Tier-1/Tier-2 wall (verdict_test green).
5. Portal accessibility computed, never stored (`reachable` is not a column anywhere).
6. The founder walked to the bar, hit the locked hatch, left through the front door, and said so.

## Self-review (done during writing)

- **Spec coverage:** §2 move (T2), §3 space (T1), §4 ObjectRelocated (T4), §5 Portal/tiers (T5, +tier1 in T1/T4), §6 tension (T3), §8 mint/three-nets (T6). §7 contract-less types unchanged (correct — F adds no math there). Every task cites its section.
- **Placeholder scan:** no TBDs; the one genuine open sub-decision (volume stored vs derived) is resolved to derived (decision 3) with the spec quote.
- **Type consistency:** `fn_distance` (T1) → `fn_move_duration_actor` (T2) → tension budget (T3); `attrs.statuses` defined T2, written T4; `contained_by`/`fn_effective_weight` (T4); `fn_portal_permits` (T5); `validateMints`/`apply_mint` (T6) — each defined once, consumed by name. `parent_location_id`/`contained_by` added to `tier1Registry` in T1/T4 respectively.
- **Blast-radius honesty:** T2 (flat-5 dependents) and T5 (cross-location move tests need portals) have the widest test fallout — both call it out and require updating fixtures (not assertions), named in the task.

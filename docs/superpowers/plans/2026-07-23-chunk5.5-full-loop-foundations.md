# Chunk 5.5 Full Loop — Program Commitment + Foundations (Plan 1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Lock the ENTIRE ruled design (FINAL set + both RULINGS files) as compiling artifacts — schemas, migrations, seat interfaces, orchestrator — so every later station fills a frozen shape and none can re-open an agreement; end with the six-type loop running end-to-end on fake drivers.

**Architecture:** The beat orchestrator moves from one SQL call into Go (`core/api`), calling SQL functions for everything deterministic (gate floor, commit, perceptions) — D-1 preserved: SQL remains the only canon writer. All LLM seats exist as bridge seats from day one (decompose, resolve, cognition-batch, cognition-isolated, world-actor, narrate); in this plan the new ones bind FAKE drivers, so CI is deterministic while the real design (no LLM-free path) keeps its sockets. Six event types replace {move, say}; attempts carry real ids.

**Tech Stack:** Go (stdlib + existing `core/api` patterns), PostgreSQL (plpgsql functions, plain-SQL test files in `core/db/tests/`), JSON Schema draft-07 (embedded, tool-use-forced structured output), existing bridge/driver registry.

## Global Constraints (the scope fence — every task implicitly includes these)

- **Scope = the corpus, exactly:** `docs/superpowers/specs/chunk-5.5-final/` (FINAL set + `RULINGS-2026-07-23-grilling-session.md` + `POST-COMPACTION-RULINGS-2026-07-03-to-22.md`). "If a computation is not in this document, the engine cannot do it." Anything not ruled → STOP and ask the founder. Do not add features, fields, or checks beyond the corpus.
- **Blocker-only:** deterministic machinery blocks impossibilities, never awards success. Commit is clerk work.
- **Measurements, not verdicts:** state stores measurements (`max_room`, `carried_weight`, `base_speed`); verdicts computed at ask-time, never stored. Nouns for state, verbs for checks. No `has_`/`can_`/`is_allowed_` columns.
- **Six event types only:** ActorMoved, Communicated, ObjectRelocated, OwnershipAccessChanged, EntityCreated/Destroyed, AttributeChanged. No `act`, no seventh type. New richness = data.
- **Decompose adds NOTHING:** stated attempts + ids from the candidate whitelist, or UNRESOLVED. No implied legs, no judgment, no tension.
- **AttributeChanged (actor-driven) = always adjudicated (v1).** No trivial-sorting anywhere.
- **Explain-first, then match:** rulings carry `reasoning` → `therefore` → `outcome`; the engine mechanically bounces `therefore`↔`outcome` mismatch (repair ×1 → bounce).
- **No LLM-free path in the DESIGN:** fakes in this plan are CI stand-ins for undelivered stations, not a design statement. Every fake's seat comment says so.
- **Secrets never enter a shared prompt** (wall by construction). One cognition call per action; exclusive batch/isolate split.
- **Append-only canon; logged once, replayed forever.** Never edit committed rows; compensating events only.
- **D-1:** only the SQL commit path writes canon. Go orchestrates; SQL commits.
- **Cache-layout rule:** prompt payloads = stable prefix + append-only events + mutable tail (never mutable data early).
- **TDD, failing test first. One station = one PR.** Plain style; every new rule in code comments ships with its example ("never confuse the implementing agent").
- **Two decisions taken by this plan (flagged, not silent):** (1) measurements live in the existing `attrs` JSONB pattern (follows shipped `actor_state.attrs.location_id` + `state_mutation.attribute_path`; the Tier-1-DDL-vs-JSONB fork stays open — revisit with running-code evidence per D-9); (2) new canon rows use the six canonical type labels; existing `move`/`private_disclosure` rows are preserved untouched (append-only) and read-mapped where needed.

---

# PROGRAM MAP (the committed whole — every station, no renegotiation)

Detailing rule: each station's plan is written when the previous station is green, grounded in the corpus + then-current code, citing rulings by section. The map itself is the commitment; a station plan may not shrink or extend it.

| Station | Delivers | Tasks (named now, detailed in that station's plan) | Ruling sources |
|---|---|---|---|
| **A–C (THIS PLAN)** | All schemas, migrations, seat interfaces, Go orchestrator, six-type loop green on fakes | Tasks 1–9 below | whole corpus |
| **D — Resolver** | Real R0–R4 on the tested model stack: gather one-hop (SQL), real adjudication, verdict (id-whitelist ∪ mint-matcher, therefore↔outcome, no-contradiction-vs-slice), repair ×1, combined ruling for collisions | D1 gather fn; D2 resolve driver + prompt (stable-prefix layout); D3 verdict+repair; D4 combined-ruling coupling; D5 bounce/failure canon tests | FINAL-resolve; RULINGS §1–§4, §9 |
| **E — Cognition** | World-first per step for real: batch seat (public moment), isolation lookup (id ∩ private subject links) + isolated seat, telegraph input, premise re-check generalized, held outcome + reaction beat | E1 batch payload+driver; E2 isolation lookup fn + isolated driver; E3 telegraph/wind-up events; E4 held-outcome beat flow; E5 wall tests (secret never in shared prompt — assert on captured prompts) | FINAL-world-npc-cognition; RULINGS §5–§6 |
| **F — Contracts + space** | move contract (nested coordinates, nearest-common-parent distance, modifiers, cumulative tension budget), ObjectRelocated (volume blocks; eager encumbrance, recursive container weight), Portal traversal, mint validation (shape + derivable bounds; three nets) | F1 coordinates + fn_distance; F2 fn_move_duration real; F3 tension budget cumulative; F4 volume/weight + eager encumbered; F5 Portal check; F6 mint validators | FINAL-action-contracts §2–§6, §8 |
| **G — World Actor** | Pending-events ledger fired on clock-crossing; pressure pools (world-time accrual, 3 magnitude tiers, logged rolls); world-scope seat authoring within drawn size | G1 ledger + crossing; G2 pools + roll log; G3 world-actor driver + payload | RULINGS §7 |
| **H — Personality** | Cores + backstory in prompts (read); per-perceiver magnitude judgment; threshold licensing; sub-threshold trait pools (evolution) | H1 core→prefix assembly; H2 magnitude in cognition output + validation; H3 threshold license + core-change commit; H4 trait pools | RULINGS §8 |
| **I — Memory + play surface** | Subject-triggered retrieval in payload assembly; narrator payload; minimal play page (dreamchat-frontend: input + narration, existing api.ts chokepoint pattern); Drowned Lantern seed world; the four-felt-properties playthrough | I1 retrieval fn + assembly; I2 narrator payload; I3 play page; I4 tavern seed + E2E "feels alive" checklist (founder plays — A7 gate) | RULINGS §10; FINAL loop PRD §5 |

---

# THIS PLAN'S TASKS (Stations A–C: schemas → migrations → interfaces → orchestrator)

**Local prerequisites:** repo `dreamchat-world-backend`, branch off `main` after PR #23 merges (or off `docs/chunk-5.5-final-set`). Confirm the DB test runner: `grep -n "test" Makefile` — use the existing target that runs `core/db/tests/*.sql`; every SQL test below follows the existing numbered-file pattern (self-contained `DO $$ ... RAISE EXCEPTION ... $$;` blocks, fresh labels, no seed dependence). Go tests: `go test ./core/api/ -run <Name> -v`.

### Task 1: beat_chain v2 — the six-type attempt schema

**Files:**
- Create: `core/api/schema/beat_chain.v2.schema.json`
- Modify: `core/api/beatvocab.go` (point at v2)
- Modify: `core/api/beatseats.go` (Attempt struct + validation)
- Test: `core/api/beatvocab_v2_test.go`

**Interfaces:**
- Produces: `type Attempt struct { Type, Stated, ToLocationID, ListenerID, Content, ObjectID, DestKind, DestID, TargetID, GranteeID, Reference string; ComponentIDs, CandidateIDs []string }` and `DecodeAndValidateChainV2(raw string) ([]Attempt, error)`. Consumed by Tasks 7–9 and every later station.
- Attempts are ATTEMPTS: they carry the player's stated words + bound ids, never outcomes (FINAL-decompose).

- [ ] **Step 1: Write the failing test**

```go
package api

import "testing"

func TestVocabularyV2IsTheSixTypesPlusUnresolved(t *testing.T) {
	want := []string{"ActorMoved", "Communicated", "ObjectRelocated",
		"OwnershipAccessChanged", "EntityCreated", "EntityDestroyed",
		"AttributeChanged", "UNRESOLVED"}
	got := vocabularyTypes()
	if len(got) != len(want) {
		t.Fatalf("vocabulary = %v, want %v", got, want)
	}
	for _, w := range want {
		if !allowedBeatTypes[w] {
			t.Fatalf("missing type %q", w)
		}
	}
}

func TestDecodeV2RejectsOutcomeShapedAndAcceptsAttempts(t *testing.T) {
	// A valid three-attempt chain (the Drowned Lantern opening).
	ok := `[{"type":"ActorMoved","stated":"I cross to the bar","to_location_id":"11111111-1111-1111-1111-111111111111"},
	 {"type":"ObjectRelocated","stated":"slip her the note","object_id":"22222222-2222-2222-2222-222222222222","dest_kind":"actor","dest_id":"33333333-3333-3333-3333-333333333333"},
	 {"type":"Communicated","stated":"ask about the harbormaster","listener_id":"33333333-3333-3333-3333-333333333333","content":"what do you know of the harbormaster?"}]`
	if _, err := DecodeAndValidateChainV2(ok); err != nil {
		t.Fatalf("valid chain rejected: %v", err)
	}
	// Old v1 shape must fail (no silent legacy acceptance).
	old := `[{"type":"move","to":"bar"}]`
	if _, err := DecodeAndValidateChainV2(old); err == nil {
		t.Fatal("v1 'move' accepted by v2 validator")
	}
	// UNRESOLVED is a first-class emission (a rejected proposal beats a guessed canon).
	unres := `[{"type":"UNRESOLVED","stated":"give the note to her","reference":"her","candidate_ids":["33333333-3333-3333-3333-333333333333","44444444-4444-4444-4444-444444444444"]}]`
	if _, err := DecodeAndValidateChainV2(unres); err != nil {
		t.Fatalf("UNRESOLVED rejected: %v", err)
	}
}
```

- [ ] **Step 2: Run, verify fail** — `go test ./core/api/ -run 'TestVocabularyV2|TestDecodeV2' -v` → FAIL (undefined).

- [ ] **Step 3: Create the schema** at `core/api/schema/beat_chain.v2.schema.json`:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "beat_chain/2 — chain of ATTEMPTS (never outcomes), ids from the candidate whitelist",
  "type": "array",
  "items": {
    "type": "object",
    "oneOf": [
      {"properties": {"type": {"const": "ActorMoved"}, "stated": {"type": "string", "minLength": 1}, "to_location_id": {"type": "string", "format": "uuid"}}, "required": ["type", "stated", "to_location_id"], "additionalProperties": false},
      {"properties": {"type": {"const": "Communicated"}, "stated": {"type": "string", "minLength": 1}, "listener_id": {"type": "string", "format": "uuid"}, "content": {"type": "string", "minLength": 1}}, "required": ["type", "stated", "listener_id", "content"], "additionalProperties": false},
      {"properties": {"type": {"const": "ObjectRelocated"}, "stated": {"type": "string", "minLength": 1}, "object_id": {"type": "string", "format": "uuid"}, "dest_kind": {"enum": ["actor", "location", "container"]}, "dest_id": {"type": "string", "format": "uuid"}}, "required": ["type", "stated", "object_id", "dest_kind", "dest_id"], "additionalProperties": false},
      {"properties": {"type": {"const": "OwnershipAccessChanged"}, "stated": {"type": "string", "minLength": 1}, "target_id": {"type": "string", "format": "uuid"}, "grantee_id": {"type": "string", "format": "uuid"}}, "required": ["type", "stated", "target_id"], "additionalProperties": false},
      {"properties": {"type": {"const": "EntityCreated"}, "stated": {"type": "string", "minLength": 1}, "component_ids": {"type": "array", "items": {"type": "string", "format": "uuid"}}}, "required": ["type", "stated"], "additionalProperties": false},
      {"properties": {"type": {"const": "EntityDestroyed"}, "stated": {"type": "string", "minLength": 1}, "target_id": {"type": "string", "format": "uuid"}}, "required": ["type", "stated", "target_id"], "additionalProperties": false},
      {"properties": {"type": {"const": "AttributeChanged"}, "stated": {"type": "string", "minLength": 1}, "target_id": {"type": "string", "format": "uuid"}}, "required": ["type", "stated", "target_id"], "additionalProperties": false},
      {"properties": {"type": {"const": "UNRESOLVED"}, "stated": {"type": "string", "minLength": 1}, "reference": {"type": "string", "minLength": 1}, "candidate_ids": {"type": "array", "minItems": 2, "items": {"type": "string", "format": "uuid"}}}, "required": ["type", "stated", "reference", "candidate_ids"], "additionalProperties": false}
    ]
  }
}
```

- [ ] **Step 4: Implement.** In `beatvocab.go`, change the embed to v2 (`//go:embed schema/beat_chain.v2.schema.json`); `vocabularyTypes()` keeps parsing `oneOf → properties.type.const` unchanged. In `beatseats.go`, add (keep `BeatStep`/v1 code untouched for replay of old worlds):

```go
// Attempt is one element of beat_chain/2 — an ATTEMPT with ids, never an
// outcome. Example: {"type":"AttributeChanged","stated":"I open the door",
// "target_id":"<door uuid>"} — what happens to the door is resolve's job.
type Attempt struct {
	Type         string   `json:"type"`
	Stated       string   `json:"stated"`
	ToLocationID string   `json:"to_location_id,omitempty"`
	ListenerID   string   `json:"listener_id,omitempty"`
	Content      string   `json:"content,omitempty"`
	ObjectID     string   `json:"object_id,omitempty"`
	DestKind     string   `json:"dest_kind,omitempty"`
	DestID       string   `json:"dest_id,omitempty"`
	TargetID     string   `json:"target_id,omitempty"`
	GranteeID    string   `json:"grantee_id,omitempty"`
	ComponentIDs []string `json:"component_ids,omitempty"`
	Reference    string   `json:"reference,omitempty"`
	CandidateIDs []string `json:"candidate_ids,omitempty"`
}

// DecodeAndValidateChainV2 is the belt behind the leash: valid JSON, every
// type in the closed six-type set (+UNRESOLVED), per-type required fields.
func DecodeAndValidateChainV2(raw string) ([]Attempt, error) {
	var chain []Attempt
	if err := json.Unmarshal([]byte(raw), &chain); err != nil {
		return nil, fmt.Errorf("chain not valid JSON: %w", err)
	}
	for i, a := range chain {
		if !allowedBeatTypes[a.Type] {
			return nil, fmt.Errorf("step %d type %q outside closed vocabulary", i, a.Type)
		}
		if a.Stated == "" {
			return nil, fmt.Errorf("step %d missing stated", i)
		}
		if err := validateAttemptFields(i, a); err != nil {
			return nil, err
		}
	}
	return chain, nil
}

// validateAttemptFields holds the per-type required-field checks; shared with
// the cognition validator (Task 3) so NPC attempts obey the same shapes.
func validateAttemptFields(i int, a Attempt) error {
	switch a.Type {
	case "ActorMoved":
		if a.ToLocationID == "" {
			return fmt.Errorf("step %d ActorMoved requires to_location_id", i)
		}
	case "Communicated":
		if a.ListenerID == "" || a.Content == "" {
			return fmt.Errorf("step %d Communicated requires listener_id+content", i)
		}
	case "ObjectRelocated":
		if a.ObjectID == "" || a.DestKind == "" || a.DestID == "" {
			return fmt.Errorf("step %d ObjectRelocated requires object_id+dest_kind+dest_id", i)
		}
	case "OwnershipAccessChanged", "EntityDestroyed", "AttributeChanged":
		if a.TargetID == "" {
			return fmt.Errorf("step %d %s requires target_id", i, a.Type)
		}
	case "UNRESOLVED":
		if a.Reference == "" || len(a.CandidateIDs) < 2 {
			return fmt.Errorf("step %d UNRESOLVED requires reference + >=2 candidates", i)
		}
	}
	return nil
}
```

- [ ] **Step 5: Run, verify pass** — same command → PASS. Also `go build ./...`.
- [ ] **Step 6: Commit** — `git add core/api && git commit -m "feat(spine): beat_chain/2 six-type attempt schema + validator"`

### Task 2: ruling/1 — the explain-first resolve output schema

**Files:**
- Create: `core/api/schema/ruling.v1.schema.json`
- Create: `core/api/ruling.go`
- Test: `core/api/ruling_test.go`

**Interfaces:**
- Produces: `type Ruling struct { Reasoning, Therefore string; Outcome RulingOutcome }`, `type RulingOutcome struct { Kind, Reason string; Events []RuledEvent; AttributeWrites []AttrWrite; Mints []json.RawMessage; Tension string }`, `type RuledEvent struct { Type, Summary string; ParticipantIDs []string; Visible bool; HiddenSummary string }`, `type AttrWrite struct { TargetID, Attribute string; Value json.RawMessage; Tier int }`, `DecodeAndValidateRuling(raw string) (Ruling, error)`. Consumed by Tasks 7, 9 and Station D.
- **The explain-first mechanics:** schema order forces `reasoning` (free text, from the gathered facts) → `therefore` (one word: `succeeds|fails|impossible`) → `outcome`. `DecodeAndValidateRuling` mechanically enforces: `impossible` ⇔ `outcome.kind=="bounce"`; `succeeds|fails` ⇔ `kind=="resolved"` with ≥1 event. This is the machine-checkable half of RULINGS §1 (the recorded bug was verdict token ≠ own reasoning); the semantic half (events contradicting the slice) is Station D's verdict stage.

- [ ] **Step 1: Failing test**

```go
package api

import "testing"

func TestRulingThereforeMustMatchOutcome(t *testing.T) {
	// The recorded failure class: reasoning concludes one way, outcome flips.
	flip := `{"reasoning":"Jonas her protector is gone; she is more vulnerable","therefore":"succeeds","outcome":{"kind":"bounce","reason":"cannot"}}`
	if _, err := DecodeAndValidateRuling(flip); err == nil {
		t.Fatal("therefore=succeeds with outcome=bounce accepted — the flip bug survives")
	}
	ok := `{"reasoning":"she is guarded; the press does not land","therefore":"fails","outcome":{"kind":"resolved","events":[{"type":"AttributeChanged","summary":"Mara hardens and deflects","participant_ids":["33333333-3333-3333-3333-333333333333"],"visible":true}]}}`
	r, err := DecodeAndValidateRuling(ok)
	if err != nil {
		t.Fatalf("valid failure ruling rejected: %v", err)
	}
	if r.Outcome.Kind != "resolved" || len(r.Outcome.Events) != 1 {
		t.Fatalf("bad decode: %+v", r)
	}
	// A failure IS an outcome and writes canon; impossibility bounces with none.
	imp := `{"reasoning":"he has no wings; flight is not possible for this actor","therefore":"impossible","outcome":{"kind":"bounce","reason":"cannot fly"}}`
	if _, err := DecodeAndValidateRuling(imp); err != nil {
		t.Fatalf("bounce ruling rejected: %v", err)
	}
}
```

- [ ] **Step 2: Run, verify fail** — `go test ./core/api/ -run TestRulingTherefore -v` → FAIL.
- [ ] **Step 3: Schema** at `core/api/schema/ruling.v1.schema.json`:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ruling/1 — reasoning FIRST, then a one-word therefore, then the outcome",
  "type": "object",
  "required": ["reasoning", "therefore", "outcome"],
  "properties": {
    "reasoning": {"type": "string", "minLength": 1},
    "therefore": {"enum": ["succeeds", "fails", "impossible"]},
    "outcome": {
      "oneOf": [
        {"type": "object", "required": ["kind", "reason"], "properties": {"kind": {"const": "bounce"}, "reason": {"type": "string", "minLength": 1}}, "additionalProperties": false},
        {"type": "object", "required": ["kind", "events"], "properties": {
          "kind": {"const": "resolved"},
          "events": {"type": "array", "minItems": 1, "items": {"type": "object", "required": ["type", "summary", "participant_ids", "visible"], "properties": {
            "type": {"enum": ["ActorMoved", "Communicated", "ObjectRelocated", "OwnershipAccessChanged", "EntityCreated", "EntityDestroyed", "AttributeChanged"]},
            "summary": {"type": "string", "minLength": 1},
            "participant_ids": {"type": "array", "minItems": 1, "items": {"type": "string", "format": "uuid"}},
            "visible": {"type": "boolean"},
            "hidden_summary": {"type": "string"}
          }, "additionalProperties": false}},
          "attribute_writes": {"type": "array", "items": {"type": "object", "required": ["target_id", "attribute", "value", "tier"], "properties": {"target_id": {"type": "string", "format": "uuid"}, "attribute": {"type": "string"}, "value": {}, "tier": {"enum": [1, 2]}}, "additionalProperties": false}},
          "mints": {"type": "array", "items": {"type": "object"}},
          "tension": {"enum": ["frantic", "tense", "normal", "calm", "none"]}
        }, "additionalProperties": false}
      ]
    }
  },
  "additionalProperties": false
}
```

- [ ] **Step 4: Implement** `core/api/ruling.go` — the structs from the Interfaces block plus:

```go
// DecodeAndValidateRuling enforces the explain-first contract mechanically:
// therefore=impossible ⇔ bounce; succeeds|fails ⇔ resolved(≥1 event).
// A FAILURE is an outcome and writes canon (the keeper hardens and lies);
// an impossibility writes nothing ("you can't fly").
func DecodeAndValidateRuling(raw string) (Ruling, error) {
	var r Ruling
	if err := json.Unmarshal([]byte(raw), &r); err != nil {
		return r, fmt.Errorf("ruling not valid JSON: %w", err)
	}
	if r.Reasoning == "" {
		return r, fmt.Errorf("ruling missing reasoning (explain-first)")
	}
	switch r.Therefore {
	case "impossible":
		if r.Outcome.Kind != "bounce" {
			return r, fmt.Errorf("therefore=impossible but outcome=%q — flip", r.Outcome.Kind)
		}
	case "succeeds", "fails":
		if r.Outcome.Kind != "resolved" || len(r.Outcome.Events) == 0 {
			return r, fmt.Errorf("therefore=%q but outcome=%q/%d events — flip", r.Therefore, r.Outcome.Kind, len(r.Outcome.Events))
		}
	default:
		return r, fmt.Errorf("therefore %q not in enum", r.Therefore)
	}
	return r, nil
}
```

- [ ] **Step 5: Run, verify pass**; `go build ./...`.
- [ ] **Step 6: Commit** — `git commit -m "feat(resolve): ruling/1 explain-first schema + therefore↔outcome match"`

### Task 3: npc_attempts/1 — the cognition output schema

**Files:**
- Create: `core/api/schema/npc_attempts.v1.schema.json`
- Create: `core/api/cognition.go`
- Test: `core/api/cognition_test.go`

**Interfaces:**
- Produces: `type NPCDecision struct { ActorID string; Reaction *NPCReaction }` (`Reaction == nil` ⇔ "none"), `type NPCReaction struct { Attempt Attempt; CommitKind string }` (`CommitKind ∈ commit|telegraph` — telegraph = the held wind-up), `DecodeAndValidateNPCDecisions(raw string, presentIDs []string) ([]NPCDecision, error)`. Consumed by Task 9 and Station E. Validation rejects any `ActorID` not in `presentIDs` (a mind can't act for someone not there) and reuses `validateAttemptFields` from Task 1 on the inner attempt.

- [ ] **Step 1: Failing test**

```go
package api

import "testing"

func TestNPCDecisionsValidateActorsAndShape(t *testing.T) {
	present := []string{"33333333-3333-3333-3333-333333333333"}
	ok := `[{"actor_id":"33333333-3333-3333-3333-333333333333","decision":"none"}]`
	ds, err := DecodeAndValidateNPCDecisions(ok, present)
	if err != nil || len(ds) != 1 || ds[0].Reaction != nil {
		t.Fatalf("none-decision: %v %+v", err, ds)
	}
	ghost := `[{"actor_id":"99999999-9999-9999-9999-999999999999","decision":"none"}]`
	if _, err := DecodeAndValidateNPCDecisions(ghost, present); err == nil {
		t.Fatal("decision for a non-present actor accepted")
	}
	tele := `[{"actor_id":"33333333-3333-3333-3333-333333333333","decision":{"commit_kind":"telegraph","attempt":{"type":"ActorMoved","stated":"Jonas pushes off the bar, moving to cut in","to_location_id":"11111111-1111-1111-1111-111111111111"}}}]`
	ds, err = DecodeAndValidateNPCDecisions(tele, present)
	if err != nil || ds[0].Reaction == nil || ds[0].Reaction.CommitKind != "telegraph" {
		t.Fatalf("telegraph decision: %v %+v", err, ds)
	}
}
```

- [ ] **Step 2: Run, verify fail.**
- [ ] **Step 3: Schema** `core/api/schema/npc_attempts.v1.schema.json` (self-contained; attempt shape mirrors beat_chain/2 without UNRESOLVED — a mind doesn't emit ambiguity about its own act):

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "npc_attempts/1 — one decision per present NPC: none, or an attempt (commit|telegraph)",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["actor_id", "decision"],
    "properties": {
      "actor_id": {"type": "string", "format": "uuid"},
      "decision": {"oneOf": [
        {"const": "none"},
        {"type": "object", "required": ["commit_kind", "attempt"], "properties": {
          "commit_kind": {"enum": ["commit", "telegraph"]},
          "attempt": {"type": "object", "required": ["type", "stated"], "properties": {
            "type": {"enum": ["ActorMoved", "Communicated", "ObjectRelocated", "OwnershipAccessChanged", "EntityCreated", "EntityDestroyed", "AttributeChanged"]},
            "stated": {"type": "string", "minLength": 1},
            "to_location_id": {"type": "string", "format": "uuid"},
            "listener_id": {"type": "string", "format": "uuid"},
            "content": {"type": "string"},
            "object_id": {"type": "string", "format": "uuid"},
            "dest_kind": {"enum": ["actor", "location", "container"]},
            "dest_id": {"type": "string", "format": "uuid"},
            "target_id": {"type": "string", "format": "uuid"},
            "grantee_id": {"type": "string", "format": "uuid"}
          }, "additionalProperties": false}
        }, "additionalProperties": false}
      ]}
    },
    "additionalProperties": false
  }
}
```

- [ ] **Step 4: Implement** `core/api/cognition.go`:

```go
// Package-internal decode for the cognition seats' output. A decision may
// only speak for a PRESENT actor, and its attempt obeys the same six-type
// field rules as the player's chain (no bypass: same shapes, same pipeline).
type NPCDecision struct {
	ActorID  string
	Reaction *NPCReaction
}

type NPCReaction struct {
	Attempt    Attempt
	CommitKind string // "commit" | "telegraph" (the held wind-up)
}

func DecodeAndValidateNPCDecisions(raw string, presentIDs []string) ([]NPCDecision, error) {
	present := make(map[string]bool, len(presentIDs))
	for _, id := range presentIDs {
		present[id] = true
	}
	var rows []struct {
		ActorID  string          `json:"actor_id"`
		Decision json.RawMessage `json:"decision"`
	}
	if err := json.Unmarshal([]byte(raw), &rows); err != nil {
		return nil, fmt.Errorf("npc decisions not valid JSON: %w", err)
	}
	out := make([]NPCDecision, 0, len(rows))
	for i, row := range rows {
		if !present[row.ActorID] {
			return nil, fmt.Errorf("decision %d for non-present actor %s", i, row.ActorID)
		}
		var none string
		if err := json.Unmarshal(row.Decision, &none); err == nil {
			if none != "none" {
				return nil, fmt.Errorf("decision %d string %q, want \"none\"", i, none)
			}
			out = append(out, NPCDecision{ActorID: row.ActorID})
			continue
		}
		var react struct {
			CommitKind string  `json:"commit_kind"`
			Attempt    Attempt `json:"attempt"`
		}
		if err := json.Unmarshal(row.Decision, &react); err != nil {
			return nil, fmt.Errorf("decision %d: %w", i, err)
		}
		if react.CommitKind != "commit" && react.CommitKind != "telegraph" {
			return nil, fmt.Errorf("decision %d commit_kind %q", i, react.CommitKind)
		}
		if react.Attempt.Stated == "" || !allowedBeatTypes[react.Attempt.Type] || react.Attempt.Type == "UNRESOLVED" {
			return nil, fmt.Errorf("decision %d attempt type %q invalid", i, react.Attempt.Type)
		}
		if err := validateAttemptFields(i, react.Attempt); err != nil {
			return nil, err
		}
		out = append(out, NPCDecision{ActorID: row.ActorID, Reaction: &NPCReaction{Attempt: react.Attempt, CommitKind: react.CommitKind}})
	}
	return out, nil
}
```

- [ ] **Step 5: Run, verify pass**; build.
- [ ] **Step 6: Commit** — `git commit -m "feat(cognition): npc_attempts/1 schema + validator (none|commit|telegraph)"`

### Task 4: Migration — locations become ids (the whitelist prerequisite)

The shipped projection stores `attrs.location_id` as a TEXT LABEL (`'tavern'`) while `entity_registry` holds location entities with uuids. Attempts bind uuids (Task 1), so location references must be uuids end to end.

**Files:**
- Create: `core/db/migrations/20260723100001_location_ids.sql`
- Modify: `core/db/schema.sql` (same changes mirrored — the repo keeps functions in both)
- Modify: `core/db/tests/91_fn_actors_at_test.sql`, `94_generate_perceptions_move_test.sql`, `95_apply_beat_happy_test.sql`, `96_apply_beat_partial_beat_test.sql` (label literals → registry uuids)
- Test: `core/db/tests/99_location_ids_test.sql`

**Interfaces:**
- Produces: `fn_actors_at(p_world_id uuid, p_location_id uuid)`; `actor_state.attrs.location_id` holds a registry uuid as text; `fn_move_duration(uuid, uuid, uuid)`. Consumed by every later station.

- [ ] **Step 1: Failing test** `core/db/tests/99_location_ids_test.sql`:

```sql
-- location ids: attrs.location_id is a registry uuid; fn_actors_at takes uuid.
DO $$
DECLARE w uuid := gen_random_uuid(); loc uuid; a uuid; n int;
BEGIN
  INSERT INTO entity_registry (entity_id, world_id, entity_kind, canonical_name)
  VALUES (gen_random_uuid(), w, 'location', 'test-vault-99') RETURNING entity_id INTO loc;
  INSERT INTO entity_registry (entity_id, world_id, entity_kind, canonical_name)
  VALUES (gen_random_uuid(), w, 'actor', 'test-kade-99') RETURNING entity_id INTO a;
  INSERT INTO actor_state (entity_id, world_id, attrs)
  VALUES (a, w, jsonb_build_object('location_id', loc::text));
  SELECT count(*) INTO n FROM fn_actors_at(w, loc);
  IF n <> 1 THEN RAISE EXCEPTION 'fn_actors_at(uuid) found % actors, want 1', n; END IF;
END $$;
```

- [ ] **Step 2: Run the DB test suite** (existing Makefile target) → FAIL (function signature is text).
- [ ] **Step 3: Migration** `20260723100001_location_ids.sql`:

```sql
-- Chunk 5.5 foundations: location references become entity_registry uuids.
-- Existing label rows are DATA in an append-only store: state_mutation history
-- keeps its old label values (history is never edited); only the LIVE
-- projection (actor_state.attrs.location_id) is converged to uuids by
-- matching canonical_name. Worlds whose labels don't match a registry
-- location keep their label (pre-spine fixtures; converge manually).
UPDATE actor_state a
SET attrs = jsonb_set(a.attrs, '{location_id}', to_jsonb(er.entity_id::text))
FROM entity_registry er
WHERE er.world_id = a.world_id
  AND er.entity_kind = 'location'
  AND er.canonical_name = a.attrs->>'location_id';

DROP FUNCTION IF EXISTS fn_actors_at(uuid, text);
CREATE FUNCTION fn_actors_at(p_world_id uuid, p_location_id uuid)
RETURNS TABLE(entity_id uuid) LANGUAGE sql STABLE AS $$
  SELECT a.entity_id FROM actor_state a
  WHERE a.world_id = p_world_id
    AND a.attrs->>'location_id' = p_location_id::text;
$$;

DROP FUNCTION IF EXISTS fn_move_duration(uuid, text, text);
CREATE FUNCTION fn_move_duration(p_world_id uuid, p_from uuid, p_to uuid)
RETURNS bigint LANGUAGE sql IMMUTABLE AS $$
  -- Flat 5-tick fixture, uuid-keyed. Station F replaces this with the
  -- nested-coordinates distance model (FINAL-action-contracts §3).
  SELECT CASE WHEN p_from = p_to THEN 0 ELSE 5 END::bigint;
$$;
```

- [ ] **Step 4: Update callers + fixtures.** Mirror both functions in `schema.sql`. In `apply_beat` (schema.sql + its migration copy), the `fn_actors_at`/`fn_move_duration` call sites now pass uuids (the SAY gate already casts `(step->>'listener')::uuid`; the MOVE path's location variables become `uuid`). Update the four listed test files: `grep -n "'tavern'\|'square'\|'vault'\|'cellar'" core/db/tests/*.sql` → each test creates its locations in `entity_registry` first and uses the returned uuids (pattern shown in Step 1).
- [ ] **Step 5: Run DB suite** → PASS (all files, including untouched invariant tests).
- [ ] **Step 6: Commit** — `git commit -m "feat(db): location references are registry uuids; fn_actors_at/fn_move_duration take uuid"`

### Task 5: Migration — six-type spine + physics vocabulary + tension + seeds

**Files:**
- Create: `core/db/migrations/20260723100002_six_type_spine.sql`
- Modify: `core/db/schema.sql` (mirror)
- Test: `core/db/tests/100_spine_seeds_test.sql`

**Interfaces:**
- Produces: `canon_event.event_type` accepts the six canonical labels (legacy rows untouched); tables `movement_type(world_id, movement_type_id, base_speed_mps)` and `status_modifier(world_id, status_type_id, action_type, movement_type_id, modifier_percent)`; `seed_world_defaults(world_id)` planting exactly `walk` 1.4 and `encumbered` −100; `location_state.attrs.tension` enum-guarded by trigger. Consumed by Stations D–H.

- [ ] **Step 1: Failing test** `core/db/tests/100_spine_seeds_test.sql`:

```sql
DO $$
DECLARE w uuid := gen_random_uuid(); n int; sp numeric;
BEGIN
  -- six canonical types are legal canon
  INSERT INTO canon_event (event_id, world_id, event_type, summary, in_world_tick, beat_seq, status, origin)
  VALUES (gen_random_uuid(), w, 'AttributeChanged', 'test row', 1, 0, 'accepted', 'freeform');
  -- seeding a world plants walk + encumbered (contracts §2: the only predefined rows)
  PERFORM seed_world_defaults(w);
  SELECT base_speed_mps INTO sp FROM movement_type WHERE world_id = w AND movement_type_id = 'walk';
  IF sp IS DISTINCT FROM 1.4 THEN RAISE EXCEPTION 'walk speed %, want 1.4', sp; END IF;
  SELECT count(*) INTO n FROM status_modifier
   WHERE world_id = w AND status_type_id = 'encumbered' AND movement_type_id = 'walk' AND modifier_percent = -100;
  IF n <> 1 THEN RAISE EXCEPTION 'encumbered seed missing'; END IF;
  -- tension is enum-guarded (engine validates membership only; mood is not arithmetic)
  BEGIN
    INSERT INTO location_state (entity_id, world_id, attrs)
    VALUES (gen_random_uuid(), w, '{"tension":"apocalyptic"}'::jsonb);
    RAISE EXCEPTION 'bad tension accepted';
  EXCEPTION WHEN raise_exception THEN
    IF SQLERRM = 'bad tension accepted' THEN RAISE; END IF; -- trigger fired = pass
  END;
END $$;
```

- [ ] **Step 2: Run** → FAIL (CHECK rejects the type; tables missing).
- [ ] **Step 3: Migration** `20260723100002_six_type_spine.sql`:

```sql
-- 1) Six canonical event types join the legal set. Legacy labels
-- ('move','private_disclosure','world_genesis') stay legal: append-only
-- history is never rewritten. New writes use canonical labels only.
ALTER TABLE canon_event DROP CONSTRAINT IF EXISTS canon_event_event_type_check;
ALTER TABLE canon_event ADD CONSTRAINT canon_event_event_type_check CHECK (event_type IN (
  'ActorMoved','Communicated','ObjectRelocated','OwnershipAccessChanged',
  'EntityCreated','EntityDestroyed','AttributeChanged',
  'move','private_disclosure','world_genesis'));

-- 2) Physics vocabulary tables (A11: engine hardcodes grammar; the LLM mints
-- typed rows). MEASUREMENTS only — no verdict columns, ever.
CREATE TABLE movement_type (
  world_id uuid NOT NULL,
  movement_type_id text NOT NULL,
  base_speed_mps numeric NOT NULL CHECK (base_speed_mps > 0),
  PRIMARY KEY (world_id, movement_type_id));

CREATE TABLE status_modifier (
  world_id uuid NOT NULL,
  status_type_id text NOT NULL,
  action_type text NOT NULL CHECK (action_type IN ('move')), -- ObjectRelocated: two fixed dims, no modes (contracts §4)
  movement_type_id text NOT NULL,
  modifier_percent numeric NOT NULL CHECK (modifier_percent >= -100), -- floor −100 (derived), NO cap (founder)
  PRIMARY KEY (world_id, status_type_id, action_type, movement_type_id),
  FOREIGN KEY (world_id, movement_type_id) REFERENCES movement_type(world_id, movement_type_id));

-- 3) Every world seeds exactly walk 1.4 + encumbered −100 (contracts §2:
-- "these two are the only predefined rows").
CREATE FUNCTION seed_world_defaults(p_world_id uuid) RETURNS void LANGUAGE sql AS $$
  INSERT INTO movement_type (world_id, movement_type_id, base_speed_mps)
  VALUES (p_world_id, 'walk', 1.4) ON CONFLICT DO NOTHING;
  INSERT INTO status_modifier (world_id, status_type_id, action_type, movement_type_id, modifier_percent)
  VALUES (p_world_id, 'encumbered', 'move', 'walk', -100) ON CONFLICT DO NOTHING;
$$;

-- 4) Tension: a scene attribute, enum-guarded at write.
CREATE FUNCTION trg_validate_tension() RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
  IF NEW.attrs ? 'tension' AND NOT (NEW.attrs->>'tension' IN ('frantic','tense','normal','calm','none')) THEN
    RAISE EXCEPTION 'tension % not in enum', NEW.attrs->>'tension';
  END IF;
  RETURN NEW;
END $$;
CREATE TRIGGER location_state_tension BEFORE INSERT OR UPDATE ON location_state
FOR EACH ROW EXECUTE FUNCTION trg_validate_tension();
```

- [ ] **Step 4: Mirror into `schema.sql`; run suite** → PASS.
- [ ] **Step 5: Commit** — `git commit -m "feat(db): six-type spine, movement/status vocabulary, walk+encumbered seeds, tension guard"`

### Task 6: Migration — personality, pools, world-actor ledger

**Files:**
- Create: `core/db/migrations/20260723100003_personality_world.sql`
- Modify: `core/db/schema.sql` (mirror)
- Test: `core/db/tests/101_personality_world_test.sql`

**Interfaces:**
- Produces (shapes frozen now; Stations G/H fill behavior):
  - `personality_core(world_id uuid, actor_id uuid PRIMARY KEY, traits jsonb NOT NULL /* {"schema_version":"traits/1",...} */, malleability numeric NOT NULL CHECK (malleability > 0 AND malleability <= 1))`
  - `trait_provenance(world_id uuid, actor_id uuid, trait_key text, event_id uuid REFERENCES canon_event(event_id), PRIMARY KEY (actor_id, trait_key, event_id))` — backstory events explaining each trait (RULINGS §8.1)
  - `trait_pool(world_id uuid, actor_id uuid, trait_key text, accrued numeric NOT NULL DEFAULT 0, threshold numeric NOT NULL, PRIMARY KEY (actor_id, trait_key))` — sub-threshold accumulation (RULINGS §8.5)
  - `pending_event(pending_id uuid PRIMARY KEY, world_id uuid, fire_at_tick bigint NOT NULL, magnitude text NOT NULL CHECK (magnitude IN ('small','medium','large')), payload jsonb NOT NULL, status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','fired','cancelled')))` — the ledger (RULINGS §7a)
  - `world_pressure(world_id uuid, tier text CHECK (tier IN ('small','medium','large')), accrued numeric NOT NULL DEFAULT 0, last_fired_tick bigint NOT NULL DEFAULT 0, PRIMARY KEY (world_id, tier))` — independent pools (RULINGS §7b)
  - `fn_due_pending(p_world_id uuid, p_tick bigint) RETURNS SETOF pending_event`

- [ ] **Step 1: Failing test** `core/db/tests/101_personality_world_test.sql` — three `DO $$` blocks in the established style: (a) insert a core (malleability 0.3) + a backstory canon event + a `trait_provenance` row and read them back; (b) insert a `pending_event` at tick 10, assert `fn_due_pending(w, 9)` returns 0 rows and `fn_due_pending(w, 10)` returns 1; (c) insert two `world_pressure` rows for the same tier and expect the second to RAISE (PK).
- [ ] **Step 2: Run** → FAIL.
- [ ] **Step 3: Write the DDL** exactly per the Interfaces block, plus:

```sql
CREATE FUNCTION fn_due_pending(p_world_id uuid, p_tick bigint)
RETURNS SETOF pending_event LANGUAGE sql STABLE AS $$
  SELECT * FROM pending_event
  WHERE world_id = p_world_id AND status = 'pending' AND fire_at_tick <= p_tick
  ORDER BY fire_at_tick;
$$;
```

- [ ] **Step 4: Mirror into schema.sql; run suite** → PASS.
- [ ] **Step 5: Commit** — `git commit -m "feat(db): personality core+provenance+pools, pending-events ledger, pressure pools"`

### Task 7: Seat interfaces + fakes — the whole cast, frozen

**Files:**
- Modify: `core/api/bridge.go` (add seats)
- Modify: `core/api/bridge_fakes.go` (fake drivers)
- Test: `core/api/bridge_seats_test.go`

**Interfaces:**
- Produces (frozen for all stations): seats `SeatResolve`, `SeatCognitionBatch`, `SeatCognitionIsolated`, `SeatWorldActor` — all `Requires: []Capability{CapStructuredOutput}`; fakes `NewFakeResolveDriver()` (returns a fixed VALID ruling/1: `therefore:"fails"`, one visible `AttributeChanged` event whose `participant_ids` echo the ids found in the prompt — same table-driven trick as the existing `fakeStructuredDriver`), `NewFakeCognitionDriver()` (returns `[]` — no present NPCs decided anything), `NewFakeWorldActorDriver()` (returns `[]`). Every fake carries the comment: `// FAKE: CI stand-in for an undelivered station. The DESIGN has no LLM-free path (POST-COMPACTION-RULINGS); this fake is scaffolding, not a design statement.`

- [ ] **Step 1: Failing test**

```go
package api

import "testing"

func TestNewSeatsBindWithCapabilityFloor(t *testing.T) {
	for _, s := range []Seat{SeatResolve, SeatCognitionBatch, SeatCognitionIsolated, SeatWorldActor} {
		if _, err := BindSeat(s, NewFakeResolveDriver()); err != nil {
			t.Fatalf("structured fake failed to bind %s: %v", s.Name, err)
		}
		if _, err := BindSeat(s, NewFakeTextDriver()); err == nil {
			t.Fatalf("text driver bound to %s — capability floor broken", s.Name)
		}
	}
}
```

- [ ] **Step 2: Run** → FAIL. **Step 3:** add the four `Seat` values next to `SeatDecompose`/`SeatNarrate` (replacing the "SeatResolve … drops in here" placeholder comment) and the three fake constructors following `fakeStructuredDriver`'s pattern.
- [ ] **Step 4: Run, pass; `go build ./...`.**
- [ ] **Step 5: Commit** — `git commit -m "feat(bridge): resolve/cognition/world-actor seats + CI fakes (capability-floored)"`

### Task 8: apply_event — one-step commit for six types (SQL stays the only writer)

**Files:**
- Create: `core/db/migrations/20260723100004_apply_event.sql`
- Modify: `core/db/schema.sql` (mirror; extend `generate_perceptions` branches)
- Test: `core/db/tests/102_apply_event_test.sql`

**Interfaces:**
- Produces: `apply_event(p_world_id uuid, p_actor_id uuid, p_attempt jsonb, p_tick bigint, p_seq int, p_origin text) RETURNS jsonb` → `{"event_id": "<uuid>"|null, "halt_reason": "committed"|"gate_reject"}`. Behavior: run the STRUCTURAL FLOOR for the attempt's type (blocker-only — contracts arrive Station F): every type: actor exists; `Communicated`: listener co-located via `fn_actors_at`; `ActorMoved`: destination exists in registry; `ObjectRelocated`: object + dest exist; others: target exists. Floor fails → `gate_reject`, NOTHING written. Floor passes → commit: `canon_event` (canonical type, `status='accepted'`), `event_participant` rows (`speaker`/`listener` for Communicated, `instigator` otherwise), `state_mutation` on `attrs.location_id` for ActorMoved, `PERFORM generate_perceptions(ev_id)`. Consumed by Task 9 + every station.
- `generate_perceptions`: `IF ev.event_type = 'move'` → `IN ('move','ActorMoved')`; `= 'private_disclosure'` → `IN ('private_disclosure','Communicated')`. Legacy rows keep replaying identically.

- [ ] **Step 1: Failing test** `102_apply_event_test.sql` — three blocks: (a) valid `Communicated` between two co-located actors → `halt_reason='committed'`, canon row `event_type='Communicated'`, speaker+listener participants, 2 perception rows; (b) `Communicated` to an actor elsewhere → `gate_reject`, canon_event count unchanged (blocker-only proof); (c) `AttributeChanged` on an existing target → committed with instigator participant (adjudication happens Go-side before apply_event ever sees a ruled event; the floor still guards existence).
- [ ] **Step 2: Run** → FAIL.
- [ ] **Step 3: Implement** `apply_event` as plpgsql extracted from `apply_beat`'s gate(3a)+apply(3c) stages, keyed by `p_attempt->>'type'` with one `CASE` for the floor and one for participant roles. Rewrite `apply_beat`'s per-step body to DELEGATE to `apply_event` (map `'say'`→`Communicated` shape, `'move'`→`ActorMoved` shape internally) — one commit path, no duplication, legacy endpoint behavior unchanged.
- [ ] **Step 4: Run the FULL suite** → PASS including untouched 91–98 (replay, orphan-free, wall, causality invariants must survive the refactor).
- [ ] **Step 5: Commit** — `git commit -m "feat(db): apply_event six-type single-step commit; blocker-only floor; perceptions accept canonical labels"`

### Task 9: The Go orchestrator — the loop shape, green on fakes

**Files:**
- Create: `core/api/orchestrator.go`
- Modify: `core/api/beathandler.go` (id-bearing candidate payload; run the orchestrator instead of the single `apply_beat` call)
- Modify: `core/api/main.go` (bind the new seats — fakes by default)
- Test: `core/api/orchestrator_test.go`

**Interfaces:**
- Produces: `type Orchestrator struct { DB <same pool type beathandler uses>; Resolve, CognitionBatch, WorldActor Driver }`, `RunBeat(ctx context.Context, worldID, actorID string, chain []Attempt) (BeatOutcome, error)`, `type BeatOutcome struct { Committed []string; HaltReason string; TicksAdvanced int64; UnresolvedCandidates []string }`.
- Per attempt, IN ORDER (FINAL loop diagram 1):
  1. **World-first hook** — call `CognitionBatch` (prompt: public moment + the telegraphed attempt + present ids; fake returns `[]`), validate via `DecodeAndValidateNPCDecisions`, commit any `commit_kind=="commit"` NPC attempts through `apply_event` (same pipeline, no bypass). Telegraphs are held (Station E completes the reaction-beat flow; foundation records the validated shape).
  2. **Premise re-check** — deterministic boolean: `Communicated` → listener still co-located (`fn_actors_at`); `ObjectRelocated` to an actor → dest actor still co-located. Flipped → stop the chain, prefix stands, `HaltReason="premise_broken"`.
  3. **Route** — `ActorMoved|Communicated|ObjectRelocated` → `apply_event` (passthrough; contract arithmetic Station F). `OwnershipAccessChanged|EntityCreated|EntityDestroyed|AttributeChanged` → **adjudicated**: gather a minimal truth slice (participants' registry rows + attrs + last-10 canon events by participation — inline SQL; Station D's `gather_slice` supersedes) → `Resolve` seat → `DecodeAndValidateRuling` → on validation error, ONE repair retry with the error text attached → second failure = bounce. `therefore=="impossible"` → `HaltReason="bounce"`, no canon, chain stops (the player rephrases — failure-is-an-answer). Otherwise commit each `Outcome.Events` element via `apply_event` (`p_origin='freeform'`).
  4. **Advance clock** — `fn_move_duration` for ActorMoved; otherwise `beat_seq` increments (shipped semantics).
  5. `UNRESOLVED` attempt → stop with `HaltReason="unresolved"` + `UnresolvedCandidates` (the clarify answer is the player's next input).
- `beathandler.go`: scene payload gains the candidate whitelist — `type Candidate struct { ID, Name, Kind string }`, `PerceptionPayload.Candidates []Candidate`, built from `fn_actors_at` + `entity_registry` (present actors, current location, artifacts there). Decompose prompt = perception lines + candidates (ids) + the v2 schema. Response becomes `{"schema_version":"beat_result/2", "narration": …, "result": {"committed": […], "halt_reason": …, "ticks_advanced": …, "unresolved_candidates": […]}}`.

- [ ] **Step 1: Failing tests** in `core/api/orchestrator_test.go` (use the DB harness pattern from the existing `beathandler_test.go`):

```go
func TestRunBeatPassthroughAndAdjudicated(t *testing.T) {
	// Seed: world, 2 locations (bar, door), player+Mara at bar, a door artifact.
	// Chain: ActorMoved(door→bar is same loc? use two locs: start at door, move to bar),
	// Communicated(ask Mara), AttributeChanged(the door) — the third routes to
	// FakeResolveDriver and commits its "fails" ruling as canon.
	// Assert: >=3 committed ids; canon rows ActorMoved, Communicated,
	// AttributeChanged exist; ticks_advanced == 5 (one real move).
}

func TestRunBeatPremiseBreakStopsChain(t *testing.T) {
	// Chain: ActorMoved(away from Mara), Communicated(ask Mara).
	// After the move, Mara is not co-located → premise re-check flips →
	// HaltReason=="premise_broken", exactly the move committed, say never ran.
}
```

Write both fully (seed helpers copied from the existing harness — same INSERT patterns as Task 4's SQL test, driven through Go).

- [ ] **Step 2: Run** → FAIL (no Orchestrator).
- [ ] **Step 3: Implement** `orchestrator.go` per the Interfaces block (~150 lines — the five-stage loop; SQL through the same pool/query style `beathandler.go` already uses).
- [ ] **Step 4: Rewire** `beathandler.go` (payload+candidates → decompose v2 → `DecodeAndValidateChainV2` → `RunBeat` → narrate from post-beat perceptions) and bind fakes in `main.go` for the three new seats.
- [ ] **Step 5: Run EVERYTHING** — `go test ./core/api/ -v` + full DB suite → PASS.
- [ ] **Step 6: Commit** — `git commit -m "feat(loop): Go orchestrator — world-first hook, premise re-check, route+adjudicate-on-fakes, clock; beat_chain/2 end-to-end"`

---

## Self-review (done during writing)

- **Spec coverage:** every corpus subsystem is either Tasks 1–9 or a named Program-Map station task with its ruling citation. Deliberate deferrals are named in the map, not silent.
- **Type consistency:** `Attempt` (T1), `Ruling` (T2), `NPCDecision` (T3), `fn_actors_at(uuid,uuid)` (T4), `seed_world_defaults` (T5), ledger/pool tables (T6), seats (T7), `apply_event` (T8), `RunBeat` (T9) — defined once, consumed by name.
- **Flagged plan-level decisions** (attrs-JSONB continuation; canonical labels beside legacy rows) sit in Global Constraints for founder review.

## Verification (station gate for THIS plan)

1. `go test ./core/api/ -v` green; full `core/db/tests/` suite green including the untouched 91–98 invariants (replay, orphan-free, wall, causality).
2. `curl -X POST .../worlds/{w}/beat` with a multi-action sentence against fake drivers returns narration + committed six-type canon rows.
3. Grep-fence: `grep -rn "has_room\|can_move\|is_allowed" core/db/` → no stored-verdict columns; `grep -rn "within-load" core/` → nothing.
4. Founder reads the Program Map and confirms it lists the FULL ruled design with nothing dropped — that is the anti-slicing contract this plan exists to keep.

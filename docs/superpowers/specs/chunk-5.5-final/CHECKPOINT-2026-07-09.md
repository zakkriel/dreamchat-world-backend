# CHECKPOINT — 2026-07-09

**Founder goal (verbatim intent):** a game that simulates a world that does NOT act around the main user,
where NOTHING is forgotten, and where each NPC is completely autonomous with an always-respected, evolving
personality.

**Confidence rates (updated after the gate+contracts pass, 2026-07-09):**
- Understanding of what Brian expects from the resolution flow: ~85% → **~88%** (learned: verdicts never stored; engine's license wider than I drew) → **~90%** after the artifact-types
  pass landed without correction (the two-tier wall was predicted, not corrected)
- Gate + action contracts: ~85% → **~92%** (within-load killed — volume blocks, weight consequences;
  eager status-write; measurements-not-verdicts core rule; folded into the doc). → **~97** after: cumulative budget confirmed, budget-as-blocker stated, AttributeChanged =
  always-adjudicated (v1, founder-ruled). Remaining ~3: encumbrance gradient (declared future) + the honest gap that no contract survives
  first contact with play unchanged. Artifact types (Portal+Container seeded, two-tier attribute wall,
  single-type v1, accessibility-not-geometry) folded into contracts §5.
- Decompose: ~80% → **~90%** (adds-nothing ruled — no implied legs, no plan-building, failure is an
  answer; wardrobe case is the proof; doc rewritten with positive/negative examples. Remaining: output
  schema, UNRESOLVED mechanics, NPC-attempts path)
- Commit / perception fan-out: ~70%
- World-first / cognition wiring: ~50%
- The resolution LLM's actual call mechanism: ~35%

**Flagged risk:** the two lowest items are where "the LLM will handle it" is doing load-bearing work — the
mirror image of the fantasy-deterministic-engine failure.

**Plan:** FIRST — create the FINAL-X files for these points (contracts complete; the rest as honest
scaffolds: locked rulings + explicit OPEN questions). SECOND — point-by-point Q&A, Claude driving the
questions, to raise each rate. Return to this checkpoint to re-measure.

---

## ADDENDUM — 2026-07-23 (after the grilling session)

The point-by-point pass ran (grill-with-docs, 2026-07-22/23). Mechanisms decided — see
`RULINGS-2026-07-23-grilling-session.md`:

- **Resolution (was ~35):** explain-first-then-match; gather = involved + one hop; naming reach =
  actor's own knowledge; bluffing via listener's knowledge; colliding attempts = one combined ruling.
  Remaining: output schema, repair-loop details, prompt/model shapes.
- **Cognition (was ~50):** one call per action; exclusive batch/isolate split on about-ness
  intersection; intent read per-NPC at resolve; world beyond the room = ledger + World Actor on
  rising world-time pressure with tiered magnitude pools; **the Personality Module** (core+backstory /
  malleability / per-perceiver magnitude / threshold / accumulation pools). Remaining: attempt-shape
  output, payload assembly, tuning values.
- **Commit/perception (was ~70):** memory scoping ruled (subject-triggered retrieval; about-ness
  hard rule at write). Remaining: fan-out fidelity, narrator payload, hidden-truth surfacing.

Rates are NOT re-scored here — per the standing rule, the rate goes up when a pass ends without a
correction, not before. Founder re-measures at the next checkpoint after reviewing the rulings file.

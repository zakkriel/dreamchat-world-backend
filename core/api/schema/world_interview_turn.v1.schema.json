{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "world_interview_turn/1",
  "title": "world_interview_turn/1 — the response to POST /worlds/interview: ONE question about a brief, or nothing left to ask. The exchange is STATELESS — the client sends the brief and every prior answer and receives one turn, so there is no session, no stored interview and nothing to resume. `done: true` arrives with no question and is a good answer, not a failure: a brief that leaves nothing undetermined should be asked nothing, and the surface always lets the user build immediately regardless.",
  "type": "object",
  "required": ["schema_version", "done"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "const": "world_interview_turn/1" },
    "done": {
      "type": "boolean",
      "description": "true when nothing is worth asking. The client stops the interview and offers the build."
    },
    "question": {
      "type": "string",
      "minLength": 1,
      "description": "Present only when done is false. Renders verbatim."
    },
    "why": {
      "type": "string",
      "minLength": 1,
      "description": "Optional single line on what this question changes about the world, shown beneath it."
    },
    "options": {
      "type": "array",
      "minItems": 1,
      "description": "Present only when done is false. Offered answers, always accompanied on the surface by a free-text field the user may type into instead — that field is a property of the surface and is deliberately not enumerated here.",
      "items": {
        "type": "object",
        "required": ["label"],
        "additionalProperties": false,
        "properties": {
          "label": { "type": "string", "minLength": 1 },
          "implication": {
            "type": "string",
            "minLength": 1,
            "description": "What choosing this would make true about the world."
          },
          "recommended": {
            "type": "boolean",
            "description": "At most one option per turn carries this. The surface highlights it as the default."
          }
        }
      }
    }
  },
  "oneOf": [
    {
      "properties": { "done": { "const": true } },
      "required": ["done"]
    },
    {
      "properties": { "done": { "const": false } },
      "required": ["question", "options"]
    }
  ]
}
